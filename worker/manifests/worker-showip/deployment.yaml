apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipa2025-msapp-worker-sh-ip-int
  namespace: ipa-2025
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipa2025-msapp-worker-sh-ip-int
  template:
    metadata:
      labels:
        app: ipa2025-msapp-worker-sh-ip-int
    spec:
      containers:
        - name: ipa2025-msapp-worker-sh-ip-int
          image: mak1z/proworker-sh_ip_int_br:latest
          envFrom:
            - secretRef:
                name: "unique-name"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
      restartPolicy: Always